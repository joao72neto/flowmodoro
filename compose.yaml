version: "3"
services:
  front:
    build:
      context: ./frontend
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"

  back:
    build:
      context: ./backend
    ports:
      - "8080:8080"